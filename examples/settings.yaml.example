# Example .amplifier/settings.yaml configuration
# Copy this file to .amplifier/settings.yaml in your project root and customize

modules:
  github:
    # === Authentication Configuration ===
    # Choose one of the following authentication methods:
    
    # Method 1: GitHub CLI (Recommended for development)
    # Run: gh auth login
    use_cli_auth: true
    
    # Method 2: Environment Variable
    # Set GITHUB_TOKEN or GH_TOKEN in your shell
    # export GITHUB_TOKEN="ghp_your_token_here"
    
    # Method 3: Direct token (not recommended - don't commit!)
    # token: ghp_your_personal_access_token_here
    
    # Prompt user if no authentication found
    prompt_if_missing: true
    
    # === GitHub Enterprise (Optional) ===
    # Uncomment and set if using GitHub Enterprise
    # base_url: https://github.company.com/api/v3
    
    # === Repository Access Control (Optional) ===
    # Leave empty or omit to allow access to all repositories
    # Add repository URLs to restrict access to specific repos
    repositories: []
    
    # Example: Restrict to specific repositories
    # repositories:
    #   - https://github.com/microsoft/vscode
    #   - git@github.com:python/cpython.git
    #   - facebook/react
    #   - your-org/your-repo
    
    # === Cross-Repository Queries ===
    # When repositories are configured, operations like list_issues and
    # list_pull_requests can query across ALL configured repos when you
    # don't provide a repository parameter!
    #
    # Example:
    # result = await amplifier.execute_tool("github", {
    #     "operation": "list_issues",
    #     "parameters": {
    #         # No "repository" parameter = queries all configured repos!
    #         "assignee": "myusername",
    #         "state": "open"
    #     }
    # })
